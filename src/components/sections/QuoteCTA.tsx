// Deprecated: superseded by InstantQuote.tsx and Partner.tsx (matches the real Birra Group design).
// Kept only because this sandbox can't delete files on the mounted folder —
// safe to delete this file by hand; it isn't imported anywhere.
export {};
